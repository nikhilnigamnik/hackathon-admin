'use strict';

/**
 * judging-criteria controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::judging-criteria.judging-criteria');
